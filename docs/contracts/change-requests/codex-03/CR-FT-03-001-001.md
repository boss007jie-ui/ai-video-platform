# CR-FT-03-001-001 — Storyboard Business Artifact and Activation Guard

## Routing

- Authorization: `FTG-0-20260720-001`
- Work item: `FT-03-001`
- Requester/workline: `codex-03`
- Owner requested: `codex-00`
- Status: `REQUESTER_COMPLETE / SHARED_DECISIONS_OPEN`
- Requester closure date: `2026-07-22`
- Foundation Registry invariant: MUST remain exactly one Envelope identity plus eleven payload Contract IDs.

## Problem

The approved Storyboard interface produces versioned Story/Scene/Beat/Shot/Panel semantics, but the current Codex-00 Business Artifact Registry has no canonical Storyboard identity. The original launch baseline also rejected implemented business Skill namespaces until Codex-00 activated its shared Architecture guard.

Codex-03 cannot change either shared surface. The branch therefore treats `artifact_kind: storyboard-owner-local-draft` as an owner-local object and exposes it through a task-owned Foundation `AssetManifest`; it does not register a new Foundation Contract or claim cross-Skill compatibility.

## Requested Codex-00 decisions and changes

1. Register one canonical Storyboard business artifact identity/version in the Business Artifact Registry outside the Foundation Registry. Codex-00 chooses the stable identity; no alias or Foundation payload ID is requested by this branch.
2. Define the minimum required fields/mapping for Story/Scene/Beat/Shot/Panel, source Contract refs/hashes, version/supersession, product identity/constraints, emotional progression, and continuity transitions.
3. Confirm whether a task-owned `AssetManifest` entry is the approved reference carrier, and define the canonical artifact URI/role vocabulary.
4. Update the Codex-00-owned Architecture/ownership guard so an authorized Skill owner branch may contain its `SKILL.md` and implementation/tests while all non-owner changes continue to fail.
5. Include the resulting registry/guard versions in Codex-00 release metadata and route Codex-06 fixtures to the frozen identity.

## Resolution ledger

| Item | State at integration `047a2c2` | Codex-03 action |
|---|---|---|
| 1. Canonical business identity | `OPEN_SHARED`: no Storyboard identity is present in `business_registry.py` | Owner-local draft identity remains explicit; no shared edit made |
| 2. Minimum canonical mapping | `OPEN_SHARED` | Current immutable artifact fields and source hashes remain available as input to Codex-00's decision |
| 3. AssetManifest carrier vocabulary | `OPEN_SHARED` | Carrier remains task-owned and marked non-registered/non-publishable |
| 4. Architecture/ownership activation | `RESOLVED_SHARED` by `79354a0`, hardened through `073ce70` | Fresh full offline suite passes the activation and bypass guards |
| 5. Integration/release routing | `OPEN_SHARED`: Storyboard code was merged through `ac1bad9`, and a generic public-interface fixture exists from `3c9ae6b`; the fixture has no frozen artifact identity | Owner tests and RC evidence refreshed after integration; no frozen-identity claim made |

## Requester closure

All Codex-03-side actions currently available without modifying shared Contracts or receiving a DV manifest are complete. Hermes accepted the Skill as `INTERNAL_PRODUCTION_READY` on 2026-07-22. That status does not authorize cross-Skill publication: items 1–3 and item 5 remain Codex-00 decisions. No Codex-03 cross-Skill identity change is valid until Codex-00 publishes such a decision. If Hermes authorizes the requested field-complete DV manifest under this work item, Codex-03 may continue only that bounded DV scope.

## Acceptance

- Foundation identity test still proves exactly 1 + 11.
- Storyboard business identity, if approved, is present only in the separate Business Artifact Registry.
- Codex-03 implementation uses the frozen identity without private cross-Skill imports.
- Shared Architecture/ownership tests accept only allowed Codex-03 paths and still reject all other owner drift.
- Codex-06 can consume the Storyboard reference through an approved public Contract/artifact mapping.

## Rollback

Codex-00 reverts only its registry/guard commit and Codex-03 remains owner-local/blocked for cross-Skill publication. No Foundation identity is renamed or removed; no Legacy or library data is touched.
