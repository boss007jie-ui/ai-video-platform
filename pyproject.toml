[build-system]
requires = []
build-backend = "ai_video_platform.build_backend"
backend-path = ["src"]

[project]
name = "ai-video-platform"
version = "0.1.0"
description = "Clean-room AI Video Platform foundation"
requires-python = ">=3.12"
dependencies = []

[tool.ai-video-platform]
authorization = "FTG-0-20260720-001"
network = "DENY_BY_DEFAULT"
foundation-registry = "IR-2 Foundation Registry V1"
