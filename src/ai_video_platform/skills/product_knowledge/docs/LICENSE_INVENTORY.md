# License Inventory

Product Knowledge `1.0.0` adds no third-party runtime or test dependency. Runtime imports are Python standard library plus in-repository `ai_video_platform.contracts` and `ai_video_platform.core` modules from the common launch baseline.

The repository does not currently declare a top-level distribution license. Release tooling must not claim an SPDX license until Codex-00 supplies the project-level license decision. No Legacy code or media was copied, wrapped, or adopted.
